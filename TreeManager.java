public class TreeManager {
    private TreeNode current = null;
    public InsertNode(String name){
        if(root == null){
            root = new TreeNode(element);
        }
        else{
            current = root;
            while(current != null){
                //if(name < current.element)
            }
        }
    }
}
